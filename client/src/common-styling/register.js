import styled from "styled-components";

const Registration = styled.div`
  &.register {
    position: absolute;
    right: 700px;
    top: 100px;
    color: white;
  }
`;

export default Registration;
